#!/usr/bin/python
#coding=utf-8

import logging
logging.basicConfig(level=logging.INFO,
    format='[%(asctime)s %(msecs)03d][%(name)-10.10s][%(funcName)-15.15s][%(levelname)s] %(message)s',
    datefmt = '%F %T')
